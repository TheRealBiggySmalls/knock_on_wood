import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swagmoneyinternational.knockonwood',
  appName: 'knock on wood',
  webDir: 'dist'
};

export default config;
